

=== INSTRUCTIONS ===

# Based on the CONTEXT above, compare 'Oldest Short Term Memory' to the 'RAG Retrieved Long Term Memories' to determine if the 'Oldest Short Term Memory' is a double up (or almost a double up, as in significantly similar). The response should be 1 word, "Yes" or "No".

If the memory is a double up (or close to it) with no new Major Important Information, then respond with exactly:

"Yes"

If it's not a double up (meaning it has new Major Important Information that should be added to the Long Term Memory), then respond with exactly:

"No"

Is the 'Oldest Short Term Memory' is a double up (or close to it)?